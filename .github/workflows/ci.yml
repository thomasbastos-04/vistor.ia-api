name: API CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore VistoriaApi.sln

      - name: Build
        run: dotnet build VistoriaApi.sln --configuration Release --no-restore

      - name: Build Docker image
        run: docker build --tag vistoria-api:ci .
